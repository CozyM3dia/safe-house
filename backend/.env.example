# Salin ke backend/.env dan isi dengan kunci milikmu sendiri.
# Jangan pernah commit file .env — hanya berkas contoh ini yang masuk repo.

# --- Database ---
# Kosongkan untuk menjalankan tanpa MongoDB. Audit tetap dihitung dan
# dikembalikan, hanya tidak tersimpan.
MONGO_URL=mongodb://localhost:27017
MONGO_DB=safe_house

# --- Server ---
PORT=8000
LOG_LEVEL=INFO

# Origin frontend yang boleh memanggil API. Pisahkan dengan koma.
CORS_ORIGINS=http://localhost:5173

# --- Kunci AI (baru dipakai pada tahap 6) ---
# Tahap 1 sampai 5 tidak membutuhkan satu pun kunci di bawah ini.
GEMINI_API_KEY=
MAPS_API_KEY=
